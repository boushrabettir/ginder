export interface User {
   /**
    * avatar_url is the profile picture url present on the users account
    */
    avatar_url: string;

    /**
     * username is the users account user name
     */
    username: string;
};
